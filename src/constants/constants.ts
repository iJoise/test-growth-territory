export enum PATH  {
   MAIN = '/',
   ADDRESS = '/address'
}

export enum ACTION_ADDRESS {
   SET_ADDRESS_INFO = 'addressReducer/SET_ADDRESS_INFO',
   SET_IS_FETCHING = 'addressReducer/SET_IS_FETCHING'
}